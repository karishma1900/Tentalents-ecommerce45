apiVersion: v1
kind: Service
metadata:
  name: { { include "Order-service.fullname" . } }
  labels: { { - include "Order-service.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "Order-service.selectorLabels" . | nindent 4 } }
